var handler = subscriptionStripeHandler(
  '<%= @coupon_form.plan.name%>',
  <%= @coupon_form.price_with_discount_in_cents %>,
  '<%= dashboard_plan_subscriptions_path(@coupon_form.plan.id) %>',
  '<%= @coupon_form.coupon_code %>',
  '<%= dashboard_spaces_path %>'
)
handler.open({});
