.container
  .row
    .col-xs-12.col-md-6.col-md-offset-3
      %h1 Edit Plan #{@plan.name}
      = simple_form_for [:dashboard, @plan] do |f|
        = f.input :name
        = f.input :number_of_publications
        = f.input :price
        = f.submit 'Update', class: 'btn btn-success'